doctype html
html(lang="en")
    head
       include partials/header
    body.login 
        .contenedor-formulario  
            h1 Inicia Sesion en UpTask 

            if(error)
                 p.alerta.error= error

            form#formulario.caja.caja-login(method="POST" action="/iniciar-Sesion")
                .campo 
                     label(for="email") E-mail :
                     input(type="email" name="email" placeholder="Tu email" value= email)
                .campo 
                     label(for="password") Password: 
                     input(type="password" name="password" placeholder="Tu password" value= password1)
                .campo.enviar 
                      input.boton(type="submit" value="Iniciar Sesion")
                .campo 
                      a(href="/crear-cuenta") ¿Aún no tienes cuenta? Regístrate

                     
